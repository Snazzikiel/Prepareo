<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.uow.snazzikiel.prepareo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".EnrolmentRecord"></activity>
        <activity android:name=".Calendar" />
        <activity
            android:name=".statistics"
            android:label="@string/title_activity_statistics"
            android:parentActivityName=".Dashboard"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.uow.snazzikiel.prepareo.Dashboard" />
        </activity>
        <activity android:name=".Goals" />
        <activity android:name=".Assignments" />
        <activity android:name=".subjectsOptions" />
        <activity android:name=".Subjects" />
        <activity android:name=".Profile" />
        <activity android:name=".notifications" />
        <activity
            android:name=".Dashboard"
            android:theme="@style/AppTheme.NoActionbar" />
        <activity android:name=".CreateAccount" />
        <activity
            android:name=".LoginPage"
            android:theme="@style/AppTheme.NoActionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".LoginPage" />
    </application>

</manifest>